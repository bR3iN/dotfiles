# Don'generate an extra prompt or Python venvs; we do this ourself in ./functions/fish_prompt
# set -x VIRTUAL_ENV_DISABLE_PROMPT yes
# set -l default_venv ~/.local/state/venv/default/bin/activate.fish
# if test -f $default_venv
#     source $default_venv
# end
