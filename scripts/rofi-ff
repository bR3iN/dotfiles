#!/usr/bin/bash

FF_DIR="$HOME/.mozilla/firefox"

PROMPT="Choose Firefox Profile"
ROFI_OPTIONS=(-no-fixed-num-lines)

list_options()
{
    awk '/Name=/{sub("Name=",""); print}' "$FF_DIR/profiles.ini"
}

callback()
{
    firefox -p "$1"
}

source ~/.config/rofi/scripts/prompt.sh
